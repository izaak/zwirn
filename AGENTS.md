Tests are selected by semantic branch, invariant, external boundary, and failure consequence. There is no test-to-production-code ratio or coverage target. Do not duplicate the same behavior at unit, integration, and CLI levels.

# Repository map

- `docs/design.md` is normative for observable Zwirn behavior.
- `docs/implementation.md` records evolving implementation decisions.
- `reference/adls-code.md` is the focused ADLS implementation reference.
